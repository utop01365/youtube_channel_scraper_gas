# セットアップ手順（5分で完了）

## ステップ1: Google スプレッドシートを作成（1分）

1. https://sheets.google.com にアクセス
2. 「新しいスプレッドシートを作成」をクリック
3. ファイル名を「YouTubeチャンネル収集」に変更

## ステップ2: Apps Script エディタを開く（30秒）

1. スプレッドシートで「拡張機能」メニューをクリック
2. 「Apps Script」を選択
3. 新しいタブで Apps Script エディタが開きます

## ステップ3: スクリプトファイルを追加（2分）

### 3-1. Config.gs を作成

1. エディタで「+」→「スクリプト」をクリック
2. ファイル名を「Config」に変更
3. このフォルダの `Config.gs` の内容をコピー＆ペースト

### 3-2. YouTubeClient.gs を作成

1. 「+」→「スクリプト」をクリック
2. ファイル名を「YouTubeClient」に変更
3. `YouTubeClient.gs` の内容をコピー＆ペースト

### 3-3. EmailExtractor.gs を作成

1. 「+」→「スクリプト」をクリック
2. ファイル名を「EmailExtractor」に変更
3. `EmailExtractor.gs` の内容をコピー＆ペースト

### 3-4. ChannelManager.gs を作成

1. 「+」→「スクリプト」をクリック
2. ファイル名を「ChannelManager」に変更
3. `ChannelManager.gs` の内容をコピー＆ペースト

### 3-5. Code.gs を上書き

1. 既存の `Code.gs` をクリック
2. すべて削除して `Code.gs` の内容をコピー＆ペースト

## ステップ4: YouTube Data API v3 の高度なサービスを有効化（1分）

### 4-1. GASエディタでサービスを追加

1. Apps Script エディタの左メニューで「サービス」（+アイコン）をクリック
2. 「YouTube Data API v3」を検索
3. 選択して「追加」をクリック

### 4-2. Google Cloud Consoleで有効化

1. https://console.cloud.google.com/ にアクセス
2. 「APIとサービス」→「ライブラリ」
3. 「YouTube Data API v3」を検索
4. 「有効にする」をクリック

## ステップ5: 初期セットアップを実行（30秒）

1. 関数選択ドロップダウン（「実行」ボタンの左）をクリック
2. `setup` を選択
3. 「実行」ボタン（▶）をクリック
4. 初回は権限の確認が表示される：
   - 「権限を確認」をクリック
   - Googleアカウントを選択
   - 「詳細」→「（プロジェクト名）に移動」をクリック
   - 「許可」をクリック
5. 実行完了を待つ（数秒）
6. スプレッドシートに戻ると「設定」シートが自動作成されています

## ステップ6: 設定シートで条件を設定（1分）

1. スプレッドシートの「設定」シートを開く
2. 以下の項目を設定：

### 基本設定
- **検索キーワード**: 「釣り」などのキーワードを入力
- **目標件数**: 1000などの数値を入力
- **メールアドレス必須**: プルダウンからTRUE/FALSEを選択
- **検索モード**: プルダウンから選択
  - channel: チャンネル直接検索（高速・低重複、推奨）
  - video: 動画経由検索（低速・高重複）

### 検索フィルタ（オプション）
- **地域コード**: プルダウンから選択（例: JP - 日本）
- **言語**: プルダウンから選択（例: ja - 日本語）
- **カテゴリID**: プルダウンから選択（例: 17 - Sports）

### チャンネル規模（オプション）
- **最小登録者数**: 例: 1000（空欄=制限なし）
- **最小動画数**: 例: 10（空欄=制限なし）
- **最小総再生回数**: 例: 10000（空欄=制限なし）

💡 **ヒント**: プルダウンメニューから簡単に選択できます！

💡 **検索順序について**: 各検索モードで、relevance（関連性）→ viewCount（視聴回数）→ date（新着）の順に自動的に検索されます。

## ステップ7: 収集を開始！（即座）

1. 関数選択ドロップダウンで `collectChannels` を選択
2. 「実行」ボタンをクリック
3. スプレッドシートに戻る
4. 「チャンネルデータ」シートで収集状況を確認

🎉 **完了！**

自動的に目標件数まで収集が継続されます。
PCを閉じても、Googleのクラウドで24時間実行されます。

## 進捗確認

### リアルタイム確認

スプレッドシートの「チャンネルデータ」シートを見るだけ！

### ログ確認

スプレッドシートの「実行ログ」シートでステータス確認

### Apps Script のログ

1. Apps Script エディタで「実行数」（時計アイコン）をクリック
2. 実行履歴が表示される

## トラブル時の確認ポイント

❌ **「YouTube Data API v3 が見つかりません」エラー**
→ ステップ4でサービスが追加されているか確認

❌ **「クォータ上限」エラー**
→ 翌朝6時に自動再開されます（メール通知あり）

❌ **権限エラー**
→ セットアップ（ステップ6）の権限許可を再実行

❌ **実行が止まる**
→ 自動的に継続されます（5秒後に再開）

## 次のステップ

✅ 目標達成を待つ（メール通知が届きます）
✅ スプレッドシートでデータを確認
✅ データをExportしてさらに分析

詳細は [README.md](README.md) または [README_GAS.md](README_GAS.md) を参照してください。
